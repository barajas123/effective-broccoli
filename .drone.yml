kind: pipeline
type: kubernetes
name: default

workspace:
  base: /drone/src
  path: .

steps:
  - name: lint
    image: python:3.12
    commands:
      - pip install black
      - black --check .

  - name: run
    image: python:3.12
    commands:
      - python hello.py
